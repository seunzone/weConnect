language: node_js
node_js:
  - "7"
env:
 MY_SECRET: secret
cache:
  directories:
    - "node_modules"
sudo: false
dist: trusty
services:
  - postgresql
before_script:
  - "psql -c 'create database wecon;' -U postgres"
  - npm run test-migrate
after_success: 
  - npm run coverage
